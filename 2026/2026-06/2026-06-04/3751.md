---
title: "Total-Waviness-Of-Numbers-In-Range-I"
question_id: "3751"
question_link: "https://leetcode.com/problems/total-waviness-of-numbers-in-range-i/description/"
difficulty: "Medium"
---



```python
class Solution:
    def totalWaviness(self, num1: int, num2: int) -> int:
        res = 0 
        for i in range(num1, num2 + 1):
            num = str(i)
            for j in range(1, len(num) - 1):
                if (num[j] < num[j - 1] and num[j] < num[j + 1] or
                    num[j] > num[j - 1] and num[j] > num[j + 1]):
                    res += 1
        return res
```

> Time Complexity : `O(n * m)`
> 
> Space Complexity : `O(1)`

---
title: "Process-String-With-Special-Operations-II"
question_id: "3614"
question_link: "https://leetcode.com/problems/process-string-with-special-operations-ii/description/"
difficulty: "Hard"
---



```python
class Solution:
    def processStr(self, s: str, k: int) -> str:
        length = 0
        for c in s:
            if c == "*":
                if length:
                    length -= 1
            elif c == "#":
                length *= 2
            elif c == "%":
                pass
            else:
                length += 1
        if k + 1 > length:
            return "."
        for c in reversed(s):
            if c == "*":
                length += 1
            elif c == "#":
                if k + 1 > (length + 1) // 2:
                    k -= length // 2
                length = (length + 1) // 2
            elif c == "%":
                k = length - k - 1
            else:
                if k + 1 == length:
                    return c
                length -= 1
        return "."
```

> Time Complexity : `O(n)`
> 
> Space Complexity : `O(1)`

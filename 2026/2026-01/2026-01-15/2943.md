---
title: "Maximize-Area-Of-Square-Hole-In-Grid"
question_id: "2943"
question_link: "https://leetcode.com/problems/maximize-area-of-square-hole-in-grid/description/"
difficulty: "Medium"
---

Just used a bunch of hints and called it a day xd

```python
class Solution(object):
    def maximizeSquareHoleArea(self, n, m, hBars, vBars):
        hBars.sort()
        vBars.sort()
        longestH, longestV = 0, 0

        seq = 0
        for i in range(len(hBars) - 1):
            if hBars[i] == hBars[i + 1] - 1:
                seq += 1
            else:
                longestH = max(longestH, seq)
                seq = 0
        longestH = max(longestH, seq)
        seq = 0

        for i in range(len(vBars) - 1):
            if vBars[i] == vBars[i + 1] - 1:
                seq += 1
            else:
                longestV = max(longestV, seq)
                seq = 0
        longestV = max(longestV, seq)
        
        maxArea = min(longestH, longestV) + 2
        return maxArea * maxArea
```

> Time Complexity : `O((n * log n) + (m * log m ))`
> 
> Space Complexity : `O(1)`
---
title: "Find-The-Largest-Area-Of-Square-Inside-Two-Rectangles"
question_id: "3047"
question_link: "https://leetcode.com/problems/find-the-largest-area-of-square-inside-two-rectangles/description/"
difficulty: "Medium"
---

I hate geometry problems.

```python
class Solution(object):
    def largestSquareArea(self, bottomLeft, topRight):
        res = 0

        for i in range(len(bottomLeft)):
            x1, y1 = bottomLeft[i]
            x2, y2 = topRight[i]

            for j in range(i + 1, len(bottomLeft)):
                x3, y3 = bottomLeft[j]
                x4, y4 = topRight[j]

                overlap_width  = min(x2, x4) - max(x1, x3)
                overlap_height = min(y2, y4) - max(y1, y3)

                side = min(overlap_width, overlap_height)
                if side > 0:
                    res = max(res, side * side)

        return res
```

> Time Complexity : `O(n^2)`
> 
> Space Complexity : `O(1)`

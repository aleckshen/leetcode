---
title: "Minimum-Distance-Between-Three-Equal-Elements"
question_id: "3740"
question_link: "https://leetcode.com/problems/minimum-distance-between-three-equal-elements-i/"
difficulty: "Easy"
---

Tripple for loop brute force solution xd

```python
class Solution:
    def minimumDistance(self, nums: List[int]) -> int:
        minDistance = inf

        for i in range(len(nums) - 2):
            for j in range(i + 1, len(nums) - 1):
                for k in range(j + 1, len(nums)):
                    if nums[i] == nums[k] == nums[j]:
                        minDistance = min(
                            minDistance,
                            abs(i - j) + abs(j - k) + abs(k - i)
                        )

        if minDistance == inf:
            return -1
        return minDistance
```

> Time Complexity : `O(n^3)`
> 
> Space Complexity : `O(1)`
